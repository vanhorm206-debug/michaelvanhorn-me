<section class="contact-band" aria-labelledby="contact-band-title">
	<div class="contact-inner wide">
		<h2 id="contact-band-title">Let's talk about AI and people.</h2>
		<div class="contact-links">
			<a href="mailto:vanhorm206@gmail.com">vanhorm206@gmail.com</a>
			<a href="https://linkedin.com/in/michaelvanhorn06">LinkedIn</a>
			<span>michaelvanhorn.me</span>
		</div>
	</div>
</section>

<style>
	.contact-band {
		background: var(--green);
		color: var(--paper);
	}

	.contact-inner {
		display: grid;
		gap: 1rem;
		padding-block: clamp(2.5rem, 6vw, 4rem);
	}

	h2 {
		color: var(--paper);
		font-size: clamp(2rem, 6vw, 3.25rem);
		max-width: 720px;
	}

	.contact-links {
		display: flex;
		flex-wrap: wrap;
		gap: 0.75rem 1.25rem;
	}

	a,
	span {
		color: var(--paper);
		font-size: 1rem;
	}
</style>
